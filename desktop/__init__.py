"""The STM Windows app: STM running on the PC with its own copy of the
hours, kept in step with the server whenever there's a connection.

Nothing in here is used by the website -- the server never imports it.
"""

# Shown in the app's Settings; keep in step with version_info.txt.
__version__ = "1.0.2"
